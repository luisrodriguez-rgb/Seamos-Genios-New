{
  "name": "seamos-genios",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve .",
    "start": "serve .",
    "build": "mkdir -p dist/css dist/js dist/assets && cp -r css/* dist/css/ && cp -r js/* dist/js/ && cp -r assets/* dist/assets/ && cp -r public/* dist/ 2>/dev/null || true && cp index.html dist/index.html && cp 404.html dist/404.html && cp favicon.ico favicon.png dist/ 2>/dev/null || true"
  }
}
